{
  "author": "DaanV2",
  "description": "Generates links using markdown tags",
  "license": "ISC",
  "main": "dist/index.js",
  "name": "markdown-tags",
  "version": "1.0.0",
  "keywords": [
    "markdown",
    "tags"
  ],
  "scripts": {
    "compile": "tsc -b && ncc build ./out/src/index.js -m -s",
    "precompile": "tsc -b --clean",
    "tags": "ts-node src/index.ts",
    "watch": "tsc -b --watch",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "fast-glob": "^3.2.12"
  },
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "@vercel/ncc": "^0.36.0",
    "jest": "^29.1.2",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
